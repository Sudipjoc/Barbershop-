// const chai = require('chai');
// const expect = chai.expect;
// const isAdmin = require('../utils/testAdmin');

// describe('isAdmin()', () => {
//   it('should return true if user has role admin', () => {
//     const user = { role: 'admin' };
//     expect(isAdmin(user)).to.be.true;
//   });

//   it('should return false if user has a different role', () => {
//     const user = { role: 'user' };
//     expect(isAdmin(user)).to.be.false;
//   });

//   it('should return false if role is missing', () => {
//     const user = {};
//     expect(isAdmin(user)).to.be.false;
//   });

//   it('should return false if user is undefined', () => {
//     expect(isAdmin(undefined)).to.be.false;
//   });
// });
